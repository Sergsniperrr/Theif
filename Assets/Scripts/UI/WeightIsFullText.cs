using DG.Tweening;
using UnityEngine;

public class WeightIsFullText : MonoBehaviour
{
  private Sequence _sequence;

  private void Start()
  {
    _sequence = DOTween.Sequence();
    
   // _sequence.Append()
  }
}